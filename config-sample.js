module.exports = {
  CONSUMER_KEY: '',
  CONSUMER_SECRET: '',
  FOLLOWING_MAX_LIMIT: 5000, // users that follows more than this limit will be desconsidered to fetch data
  APPEARENCES_COUNT_MIN_LIMIT: 70 // how many times a user needs to appear to be included on the results ('1' includes every user)
}
